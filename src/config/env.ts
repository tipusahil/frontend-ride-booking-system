const config = {
  baseUrl: import.meta.env.VITE_BASE_URL || import.meta.env.BACKEND_BASE_URL,
};
export default config;
// const config = {
//     // baseUrl : import.meta.env.VITE_BASE_URL,
// baseUrl: import.meta.env.VITE_BACKEND_BASE_URL || import.meta.env.VITE_BASE_URL,// vite project e baseUrl er namer shurote (vite) likte  hobe evabe (VITE_BACKEND_BASE_URL) eta ekta vite bittik project er convention , tai follow korte hbe
//     // baseUrl : import.meta.env.VITE_BACKEND_BASE_URL
// };

// export default config;
